# Example export fragment
#
# ROLE: Reusable template / example only.
# NOT loaded by configs/training/coding/.
# Production export lives in training/coding/export.yaml.

export_types:
  - peft_adapter
  - manifest
