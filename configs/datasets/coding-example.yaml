# Example dataset mix fragment (paths are placeholders — loading arrives in Phase 1)
datasets:
  - name: coding
    path: ../datasets/coding_v1_0.jsonl
    dataset_type: coding
    protocol_version: "1.0"
    weight: 1.0
shuffle: true
seed: 42
