# Example dataset mix fragment
#
# ROLE: Example / test fixture pointer only.
# NOT loaded by configs/training/coding/.
# Consumed only when explicitly included (e.g. configs/experiments/example.yaml).

datasets:
  - name: coding
    path: ../../tests/fixtures/datasets/coding.jsonl
    dataset_type: coding
    protocol_version: "1.0"
    weight: 1.0
shuffle: true
seed: 42
