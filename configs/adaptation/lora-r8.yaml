# Example adaptation profile fragment
#
# ROLE: Reusable template / example only.
# NOT loaded by configs/training/coding/.
# Consumed only when explicitly included (e.g. configs/experiments/example.yaml).

adapter_type: lora
rank: 8
alpha: 16
dropout: 0.05
target_modules:
  - q_proj
  - k_proj
  - v_proj
  - o_proj
