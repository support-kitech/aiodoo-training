# Example training hyperparameters fragment
#
# ROLE: Reusable template / example only.
# NOT loaded by configs/experiments/production/EXP-0001/.
# Consumed only when explicitly included (e.g. configs/experiments/example.yaml).

learning_rate: 0.0002
weight_decay: 0.0
warmup_ratio: 0.03
num_epochs: 1.0
per_device_batch_size: 1
gradient_accumulation_steps: 4
