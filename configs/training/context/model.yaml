# ============================================================
# context — Base model (FW0 System Spine)
# ============================================================
# Development Foundation (frozen DS-3.1).
# Optional Product binding of context is finalized in FW3; FW0 assigns the
# Development foundation Hub id so the spine has a concrete identity.
# ============================================================

model:
  identifier: deepseek-ai/DeepSeek-Coder-V2-Lite-Instruct
  family: deepseek-coder
  precision: fp16
  backend: hf_causal
  tokenizer_binding: deepseek-coder
