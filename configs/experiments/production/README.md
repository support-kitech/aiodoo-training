# Legacy experiment configs (migrated)

Production training configurations moved to semantic paths:

```text
configs/training/coding/
```

Legacy internal id `EXP-0001` maps to training id `coding`.

Do not create new `EXP-*` directories here. Use `configs/training/<training_id>/`.
