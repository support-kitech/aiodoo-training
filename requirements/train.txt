# Optional ML / tokenizer training dependencies.
#
# Boundary rules:
#   requirements/base.txt  — config, YAML, pydantic (always)
#   requirements/train.txt — optional ML libs (this file)
#   requirements/dev.txt   — base + pytest/ruff/mypy (CI)
#
# CI installs ONLY requirements/dev.txt (CPU) and uses StubModelBackend /
# DeterministicStubTokenizer. Never add CUDA wheels to CI.
#
# Phase 1: transformers optional for HuggingFaceTokenizerAdapter.
# Phase 2: torch + peft optional for HuggingFaceCausalLMBackend / LoRA / QLoRA.
# Install locally when exercising real backends:
#   pip install -r requirements/train.txt
transformers>=4.40.0
torch>=2.2.0
peft>=0.11.0

# Optional (QLoRA 4/8-bit); never imported outside infrastructure:
# bitsandbytes>=0.43.0

# Deferred to later phases:
# accelerate
# deepspeed
